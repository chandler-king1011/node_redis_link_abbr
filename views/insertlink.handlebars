<style>
    body {
        margin: 0;
        background-color: #EFF3F9;
    }
    .home-wrapper > .heading-wrapper {
        display: flex;
        justify-content: space-evenly;
        align-items: center;
        background-color: #0066ffff;
    }
    .heading-wrapper h1 {
        color: #00ff00ff;
    }
    .heading-wrapper a {
        text-decoration: none;
        color: #00ff00ff;
        font-size: 1.25em;
        transition: 0.2s ease-in-out;
    }
    .heading-wrapper a:hover {
        color: #EFF3F9;
    }
    .body-wrapper {
        margin-top: 150px;
        display: flex;
        align-items: center;
        flex-direction: column;}
    form {
        height: 50vh;
        width: 50vw;
        display: flex;
        justify-content: space-around;
        align-items: center;
        background-color:  #0066ffff;
        box-shadow: 0px 7px 10px 0px rgba(0,34,85,1);
    }
    form > .inputs {
        height: 150px;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
    
    }
    form input {
        
        width: 500px;
        height: 50px;
        border: none;
        transition: 0.2s ease-in-out;
        background-color: #d5e5ffff;
        border-radius: 2px;
    }
    form input:focus {
        outline: none;
        border-bottom: solid 1px #0066ffff;
        box-shadow: -1px 9px 24px 0px rgba(0,34,85,0.64);
    }

    form > .button {
        width: 400px;
        display: flex;
        justify-content: center;
        align-items: center;
    }
    form > .button button {
        height: 60px;
        width: 160px;
        border-radius: 5px;
        border-color: #00ff00ff;
        background-color:  #00ff00ff;
        color:  #002255ff;
        box-shadow: -1px 9px 24px 0px rgba(0,34,85,0.64);
    }
    .body-wrapper > .new_url {
        display: flex;
        align-items: center;
        justify-content: center;
        height: 100px;
        width: 50vw;
        background-color:  #00ff00ff;
        color:  #002255ff;
        font-size: 1.5em;
        margin-top: 100px;
    }
 

</style>

<div class="home-wrapper">
        <div class = "heading-wrapper">
            <h1>Redis Link Shortener</h1>
            <a href="/links">View All Links</a>
        </div>
        <div class="body-wrapper">
            <form method="POST" onSubmit= action="/">
                <div class="inputs">
                    <input type="text" name = "id" placeholder="ID" />
                    <input type="text" name = "url" placeholder="URL"/>
                </div>
                <div class= "button">
                    <button type="submit">Shorten URL</button>
                </div>
            </form>
            {{#if link}}<div class="new_url">Your shortened link is.. {{link}}</div>{{/if}}
        </div>

</div>